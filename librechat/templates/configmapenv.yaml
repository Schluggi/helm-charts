apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "librechat.fullname" . }}
  labels:
    {{- include "librechat.labels" . | nindent 4 }}
data:
{{- range $key, $val := fromYaml .Values.config }}
  {{ $key }}: {{ $val | quote }}
{{- end }}